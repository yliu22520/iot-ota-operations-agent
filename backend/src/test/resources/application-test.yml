# The real model key is supplied only when an explicit Gemini evaluation is run.
# The selected chat model is created by the provider adapter only when that evaluation is enabled.
spring:
  ai:
    model:
      chat: none
